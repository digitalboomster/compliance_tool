services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: compliance
    ports:
      - "5432:5432"
    volumes:
      - savvybee_pgdata:/var/lib/postgresql/data

volumes:
  savvybee_pgdata:
